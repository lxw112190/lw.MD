# Keep repository text deterministic across Windows and Linux runners.
* text=auto eol=lf

# Prefer native Windows line endings for PowerShell scripts.
*.ps1 text eol=crlf

# Binary assets must never be line-ending normalized.
*.ico binary
*.jpg binary
*.png binary
*.zip binary
*.gz binary
